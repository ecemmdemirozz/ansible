--- 
  - name: Selinux
    hosts: localhost
    tasks: 
      - name: Selinux disabled
        lineinfile:
          path: /etc/selinux/config
          regexp: "^SELINUX="
          line: "SELINUX=disabled"
      - name: Reboot system
        command: reboot